{
  "name": "lookup-server",
  "version": "0.0.1",
  "devDependencies": {
    "concurrently": "^4.1.0",
    "dotenv": "^7.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "request": "^2.88.0"
  },
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server.js",
    "client": "cd client && npm run start"
  }
}
